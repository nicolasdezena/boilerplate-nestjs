import { Injectable } from '@nestjs/common';
import { InjectModel } from '@nestjs/mongoose';
import { Model, Types } from 'mongoose';
import { {{pascalCase name}}, {{pascalCase name}}Document } from './{{lowerCase name}}.entity';

@Injectable()
export class {{pascalCase name}}Repository {
  constructor(
    @InjectModel({{pascalCase name}}.name)
    private {{camelCase name}}Model: Model<{{pascalCase name}}Document>
  ) {}
}
