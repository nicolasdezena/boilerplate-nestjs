import {{camelCase name}} from './{{camelCase name}}';

describe('{{camelCase name}} Util', () => {
  it('should return value correctly', () => {
    const output = {{camelCase name}}({ value: 'test' });
    expect(output).toBe('test');
  });
});
